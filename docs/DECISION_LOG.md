# Decision Log

| Date | Decision | Rationale | Consequences |
|---|---|---|---|
| 2026-05-21 | Gate 1 work is limited to audit, provenance, and skeleton data products. | Charter blocks modeling until dataset evidence is reproducible. | No training loops or model code in initial repo bootstrap. |
| 2026-05-21 | Inventory and manifest commands must run without real dataset files. | Dataset may not be downloaded yet. | Missing data roots produce provenance-bearing empty audit outputs. |
| 2026-05-22 | LOG_AGE is accepted for the first interval-exposure MVP. | `modality_table_log_age.parquet` is ingested with 904,977,105 rows, while raw LOG traceability is still needed for final exposure claims. | Interval features may use LOG_AGE exposure summaries, but inserted diagnostics are masked and counted only as leakage-risk QA signals. |
| 2026-05-22 | Split registry provenance is part of the interval table. | Frozen grouped splits are required before baseline evidence is meaningful. | `interval_table.parquet` records the split registry SHA-256 in Parquet metadata. |
| 2026-05-22 | LOG_AGE monotonicity violations are triaged and propagated to interval quality flags. | Strict full-data QA found 7,107 decreases; the detailed default-tolerance report records 7,071 EFC-only violations across 207 cells and maps them to 1,054 interval rows. | Clean-baseline training remains blocked until the handling policy decides whether flagged intervals are excluded, tolerated, or used only in sensitivity analysis. |
| 2026-05-22 | Headline split-registry OOD folds must be audited before baselines. | The hot-temperature holdout is 40 C and the high-C-rate holdout includes 5/3 C, not 45 C or >=2 C. | `reports/audit/split_registry_report.json` must pass and confirm non-empty headline OOD folds before baseline reports. |
| 2026-05-22 | Milestone 0.4 defines tolerant and strict interval subsets before baselines. | Default-tolerance LOG_AGE monotonicity violations are tiny EFC-only drops under 0.00025 EFC, while timestamp drops and large EFC drops remain hard exclusions. | `interval_subset_registry_v1.parquet` is the source of truth for baseline-clean interval eligibility; no model training is authorized until downstream code consumes it. |
| 2026-05-22 | Voltage/SOC-window holdout semantics use voltage-window families. | Scalar `age_soc` does not represent cyclic voltage windows; CFG provides `V_min_cyc_V` and `V_max_cyc_V`. | Split audit now fails if corrected voltage-window holdout folds are empty. |
| 2026-05-22 | Milestone 0.5 starts with scalar capacity baselines only. | The interval subset registry now defines the clean/tolerant populations, so the next scientific gate is a deliberately small capacity ladder before multimodal claims. | `mbp baseline run-capacity` must consume `interval_subset_registry_v1.parquet`, report strict-vs-tolerant sensitivity, aggregate metrics by parameter set, and avoid EIS, PULSE, sequence, neural, policy-ranking, or CBAT work. |
| 2026-05-22 | Learned capacity baselines include prior capacity state. | Predicting `capacity_Ah_k1` without `capacity_Ah_k` makes L1-L3 artificially weak; prior check-up capacity is observed before the forecast interval and is not leakage. | `F0_time_only` remains a weak sanity baseline, while `F1_state_time` through `F4_state_log_age_scalar` include `capacity_Ah_k`. |
| 2026-05-22 | Milestone 0.5b reviews and hardens scalar capacity baselines before expanding scope. | The first bounded L0-L3 report showed C-rate holdout is hardest and LOG_AGE scalar gains are mixed, so adding modalities now would obscure the capacity-baseline interpretation. | Add diagnostics, train-fold Ridge scaling, quantile metrics, and focused HGB robustness; continue blocking EIS/PULSE, knee prediction, sequence models, neural models, policy ranking, and CBAT. |
