# Validation Protocol

No model validation is authorized during Gate 1.

Required future split discipline:

- Primary evidence uses condition-level grouped splits.
- Replicates from the same condition remain together for headline claims.
- Random row/cell splits are leakage smoke tests only.
- LOG_AGE inserted diagnostics must be masked for interval prediction.
- Split registries must be versioned before baseline leaderboard work begins.

