# Schema Registry

Schema version prefix: `gate1.audit.v1`

| Data product | Status | Owner module | Required provenance |
|---|---|---|---|
| `file_inventory` | Implemented | `mbp.audit.inventory` | data root, tool, generated timestamp, schema version, SHA-256 |
| `DATASET_MANIFEST.json` | Implemented | `mbp.audit.manifest` | data root, tool version, preprocessing commit, generated timestamp |
| `MANIFEST.sha256` | Implemented | `mbp.audit.manifest` | relative path and SHA-256 per observed file |
| `modality_coverage.csv` | Placeholder implemented | `mbp.audit.coverage` | modality family, observed files, rows, status |
| `known_issues.csv` | Placeholder implemented | `mbp.audit.known_issues` | issue ID, severity, status, evidence |
| `cell_condition_table` | Implemented | `mbp.data.luh_blank.parse_cfg` | source files, parser version, schema version, voltage-window family |
| `checkup_event_table` | Implemented | `mbp.data.luh_blank.parse_eoc` | source files, parser version, schema version |
| `run_event_table_v1` | Implemented | `mbp.data.products.run_events` | interval table path, LOG_AGE path, current-sign policy, schema version |
| `modality_table_eis` | Implemented | `mbp.data.luh_blank.parse_eis` | source files, parser version, EIS validity masks |
| `modality_table_pulse` | Implemented | `mbp.data.luh_blank.parse_pulse` | source files, parser version, pulse provenance |
| `modality_table_log_age` | Implemented | `mbp.data.luh_blank.parse_log` | source archive, source file, cohort exclusion report, diagnostic masking rules |
| `split_registry_v1` | Implemented | `mbp.data.splitting` | condition table, deterministic seed, grouped parameter-set folds, voltage-window holdout |
| `interval_table` | Implemented MVP | `mbp.data.products.interval_table` | joined inputs, split registry SHA-256, LOG_AGE row-group exposure scan, monotonicity report path, leakage checks |
| `interval_subset_registry_v1` | Implemented | `mbp.data.products.interval_subsets` | interval table path, LOG_AGE monotonicity policy version, EFC jitter threshold, schema version |
| `interval_stress_features_v1` | Implemented | `mbp.data.products.stress_features` | interval table path, LOG_AGE path, row-group stress scan, feature policy version, current-sign policy |
| `current_sign_audit_report` | Implemented | `mbp.data.products.current_sign_audit` | LOG_AGE path, interval table path, streamed row-group derivative evidence, sign convention recommendation |
| `interval_stress_features_v1_1` | Implemented | `mbp.data.products.stress_features` | interval table path, LOG_AGE path, timestamp-weighted row-group stress scan, coverage/gap diagnostics, event summaries, feature policy version, current-sign policy |
| `capacity_baseline_predictions` | Implemented | `mbp.baselines.capacity` | interval table path, interval subset registry path, split view, subset, model level, feature group, schema version |
| `capacity_baseline_report` | Implemented | `mbp.baselines.capacity` | interval table path, interval subset registry path, optional stress-feature sidecar path, subset, model level, feature group, split view, strict/tolerant sensitivity scope, L3 noncrossing quantile endpoint policy |
| `stressor_robust_capacity_report` | Implemented | `mbp.baselines.stressor_robust_capacity` | interval table path, interval subset registry path, optional stress-feature sidecar path, robust model level, feature group, grouped split view, strict/tolerant sensitivity scope |
| `stressor_robust_pareto_report` | Implemented | `mbp.baselines.stressor_robust_capacity` | interval table path, interval subset registry path, optional stress-feature sidecar path, robust model setting, feature group, grouped split view, weight strength, bag count, predeclared guardrail |
| `stressor_robust_adaptive_report` | Implemented | `mbp.baselines.stressor_robust_capacity` | interval table path, interval subset registry path, optional stress-feature sidecar path, feature group, outer grouped split view, inner selection split views, selection policy, selected weight strength, unchanged non-degradation guardrail |
| `stressor_robust_attribution_report` | Implemented | `mbp.baselines.stressor_robust_capacity` | interval table path, interval subset registry path, stress-feature sidecar path, attribution arm, feature group, adaptive selection policy, grouped split view, unchanged non-degradation guardrail |
| `stressor_robust_arm_selector_report` | Implemented | `mbp.baselines.stressor_robust_capacity` | attribution report path, attribution prediction path, D0/D2 stressor-family router, C-rate selector gain, outside-C-rate non-degradation, leakage audit |
| `hierarchical_replicate_capacity_report` | Implemented | `mbp.baselines.hierarchical_capacity` | interval table path, interval subset registry path, optional stress-feature sidecar path, hierarchical model level, feature group, grouped split view, train-only residual pools, replicate interval radius, leakage audit |
| `capacity_baseline_diagnostics` | Implemented | `mbp.baselines.capacity` | baseline report path, optional L0 reference report path, feature-gain diagnostics, best-by-target/split rows, C-rate holdout condition/grouped errors, claim-readiness memo, noncrossing quantile metrics |
| `stress_feature_diagnostics` | Implemented | `mbp.baselines.capacity` | stress-feature baseline report path, HGB-50 F4 baseline report path, L0 reference report path, C-rate success criteria |
| `target_consistency_diagnostics` | Implemented | `mbp.baselines.capacity` | capacity report path, prediction Parquet path, interval-table join, direct-vs-derived target metrics, C-rate residual groupings, stress-feature ablation gains |
| `c_rate_delta_failure_diagnostics` | Implemented | `mbp.baselines.capacity` | normalized delta-rate target report, train-fold residual correction report, narrow F11-F13 feature report, direct F4 threshold comparison |
| `pulse_qa_report` | Implemented | `mbp.data.products.pulse_targets` | PULSE summary path, check-up table path, canonical target policy, coverage CSV, alignment report |
| `pulse_target_table` | Implemented | `mbp.data.products.pulse_targets` | PULSE summary path, interval table path, canonical SOC/temperature context, policy version, schema version |
| `pulse_alignment_sensitivity_report` | Implemented | `mbp.data.products.pulse_targets` | PULSE summary path, pulse target table path, interval table path, canonical context, alignment thresholds, retained coverage |
| `pulse_missingness_reports` | Implemented | `mbp.data.products.pulse_targets` | PULSE target table path, interval table path, missing endpoint rows, condition groupings, split groupings |
| `pulse_resistance_baseline_report` | Implemented | `mbp.baselines.pulse` | interval table path, interval subset registry path, PULSE target table path, optional stress-feature sidecar, grouped split view, target coverage |
| `pulse_target_robustness_report` | Implemented | `mbp.baselines.pulse` | PULSE baseline report path, scalar target list, grouped split view, 1s/10ms comparisons, delta/k1 comparisons, claim-readiness memo |
| `capacity_pulse_coupling_table` | Implemented | `mbp.coupling.pulse_capacity` | interval table path, PULSE target table path, canonical target policy, schema version |
| `pulse_capacity_coupling_diagnostics` | Implemented | `mbp.coupling.pulse_capacity` | capacity report path, capacity prediction path, interval table path, PULSE target table path, residual/PULSE-growth correlation outputs |
| `pulse_capacity_coupling_robustness` | Implemented | `mbp.coupling.pulse_capacity` | canonical model/feature/target selection, interval-level aggregation, condition-level aggregation, parameter-set bootstrap, residualized confound-control diagnostics |
| `prior_pulse_capacity_predictive_comparison` | Implemented | `mbp.baselines.capacity` | capacity baseline report, prior-PULSE capacity report, paired condition-level gains, parameter-set bootstrap, coverage effects, leakage audit |
| `prior_pulse_vs_best_nonpulse_comparison` | Implemented | `mbp.baselines.capacity` | non-PULSE capacity reports, prior-PULSE capacity report, PULSE-covered interval restriction, strongest non-PULSE group selection, paired gains, bootstrap intervals |
| `eis_qa_report` | Implemented | `mbp.data.products.eis_features` | EIS table path, spectrum-quality path, interval table path, coverage CSV, alignment report, valid-frequency mask audit |
| `eis_feature_table_v1` | Implemented | `mbp.data.products.eis_features` | EIS table path, spectrum-quality path, interval table path, canonical SOC/temperature context, feature policy version, schema version |
| `eis_feature_qa_report` | Implemented | `mbp.data.products.eis_features` | EIS feature table path, interval table path, selected-frequency completeness, feature NaN counts, split coverage |
| `eis_claim_readiness` | Implemented | `mbp.data.products.eis_features` | EIS QA report, EIS feature QA report, feature policy status, blocked predictive claims |
| `eis_target_table_v1` | Implemented | `mbp.data.products.eis_features` | EIS feature table path, interval table path, canonical SOC/temperature context, k/k1 feature availability, delta EIS targets, feature policy version |
| `eis_scalar_baseline_report` | Implemented | `mbp.baselines.eis` | interval table path, interval subset registry path, EIS target table path, grouped split view, target coverage, leakage audit |
| `prior_eis_vs_best_noneis_comparison` | Implemented | `mbp.baselines.eis_claims` | non-EIS report paths, prior-EIS report path, EIS target table path, same-population filters, paired condition gains, parameter-set bootstrap intervals |
| `eis_alignment_feature_sensitivity` | Implemented | `mbp.baselines.eis_claims` | EIS target table path, prior-EIS/non-EIS report paths, alignment thresholds, feature-completeness filters, retained coverage counts |
| `eis_leakage_audit` | Implemented | `mbp.baselines.eis_claims` | capacity/PULSE/EIS feature group definitions, prior-EIS allowlist, future EIS/R0R1/DRT/embedding blocklist |
| `semi_empirical_capacity_report` | Implemented | `mbp.baselines.semi_empirical` | interval table path, interval subset registry path, stress-feature sidecar path, SE0-SE4 feature policy, grouped split view |
| `replicate_uncertainty_diagnostics` | Implemented | `mbp.analysis.replicate_uncertainty` | interval table path, capacity report path, capacity prediction path, condition-triplet spread, empirical tolerance intervals |
| `capacity_calibration_diagnostics` | Implemented | `mbp.analysis.calibration` | capacity report path, capacity prediction path, replicate-spread table, grouped calibration method, no-test-leakage calibration source |
| `interval_sequence_features_v1` | Implemented | `mbp.data.products.run_events` | run-event table path, interval table path, shuffle seed, feature policy version, schema version |
| `interval_event_sequence_table_v1` | Implemented | `mbp.data.products.event_sequences` | run-event table path, interval table path, max events, deterministic shuffle seed, fixed-length true/shuffled event vectors, event mask, feature policy version, schema version |
| `minimal_sequence_reopening_report` | Implemented | `mbp.baselines.minimal_sequence` | interval table path, interval subset registry path, event-sequence table path, grouped split view, Ridge true/shuffled rows, CUDA-only Torch MLP true/shuffled rows, aggregate/stress reference reports, leakage audit, neural environment evidence |
| `policy_contrast_registry_v1` | Implemented | `mbp.analysis.policy_contrast` | interval table path, matched contrast family, varied field, matched nominal fields, triplet support, common check-up support, schema version |
| `policy_contrast_support_diagnostics` | Implemented | `mbp.analysis.policy_contrast` | policy contrast registry path, interval table path, family support rows, observed capacity-loss sign-stability rows, claim-readiness |
| `policy_ranking_feasibility_diagnostics` | Implemented | `mbp.analysis.policy_contrast` | policy contrast registry path, capacity horizon table path, capacity horizon prediction path, selected targets/horizons/splits/models/features, pairwise sign/magnitude rows, contrast-bootstrap rows, claim-readiness |
| `sequence_value_diagnostics` | Implemented | `mbp.baselines.sequence_value` | sequence-value capacity report path, stress baseline report path, aggregate/order/shuffled comparisons, claim-readiness |
| `knee_candidate_table_v1` | Implemented | `mbp.analysis.knee` | interval table path, detector policy, x-axis policy, smoothing policy, schema version |
| `knee_risk_label_table_v1` | Implemented | `mbp.analysis.knee` | knee candidate table path, interval table path, primary detector policy, exploratory label status, schema version |
| `knee_label_stability_diagnostics` | Implemented | `mbp.analysis.knee` | candidate knee table path, detector agreement, x-axis sensitivity, smoothing sensitivity, replicate consistency, claim-readiness |
| `knee_stable_condition_registry_v1` | Implemented | `mbp.analysis.knee` | candidate knee table path, interval table path, primary detector policy, stable-condition rule, schema version |
| `threshold_event_label_table_v1` | Implemented | `mbp.analysis.knee` | interval table path, threshold fractions, SOH/capacity-relative threshold policy, exploratory label status, schema version |
| `knee_threshold_label_forensics` | Implemented | `mbp.analysis.knee` | candidate knee table path, interval table path, inconsistent-condition rows, stable registry, threshold stability, knee-vs-threshold decision |
| `threshold_warning_table_v1` | Implemented | `mbp.analysis.knee` | threshold event label path, interval table path, selected threshold, prior-state feature policy, schema version |
| `threshold_warning_baseline_report` | Implemented | `mbp.baselines.threshold_warning` | threshold warning table path, B0-B6 non-neural/proximity models, W0-W2 prospective feature groups, grouped split view, leakage audit, fixed-width and equal-frequency ECE |
| `threshold_warning_hardening_diagnostics` | Implemented | `mbp.baselines.threshold_warning` | threshold warning report path, prediction path, warning table path, lead-time bins, proximity bins, censoring policy, reliability bins, fixed-width and equal-frequency calibration by split |
| `threshold_warning_censoring_sensitivity` | Implemented | `mbp.baselines.threshold_warning` | all-row report path, verified-only report path, label-policy definitions, metric comparison, split comparison, C-rate comparison |
| `threshold_warning_final_claim_readiness` | Implemented | `mbp.baselines.threshold_warning` | threshold warning report path, prediction path, warning table path, censoring sensitivity summary, final lead-time and C-rate claim matrices, fixed-width and equal-frequency ECE |
| `threshold_warning_probability_calibration` | Implemented | `mbp.baselines.threshold_warning` | warning table path, HGB W2 base model, grouped fit/calibration/test condition partition, label policy, calibration method, leakage audit, fixed-width and equal-frequency ECE |
| `capacity_horizon_table_v1` | Implemented | `mbp.analysis.capacity_horizon` | interval table path, horizon list, prior-state feature policy, oracle future-exposure diagnostic field policy, schema version |
| `capacity_horizon_trajectory_features_v1` | Implemented | `mbp.analysis.capacity_horizon` | capacity horizon table path, interval table path, prior-only capacity trajectory features, leakage policy, schema version |
| `capacity_horizon_baseline_report` | Implemented | `mbp.baselines.capacity_horizon` | capacity horizon table path, horizons, non-neural baseline IDs, K0-K3 feature groups, grouped split view, leakage audit, claim-readiness |
| `capacity_horizon_forensics_report` | Implemented | `mbp.baselines.capacity_horizon` | capacity horizon report path, prediction path, horizon table path, split/reference gains, C-rate condition errors, prior-slope failure modes, oracle exposure gain audit |
| `capacity_horizon_trajectory_diagnostics` | Implemented | `mbp.baselines.capacity_horizon` | trajectory baseline report path, prediction path, horizon table path, K4/K5 comparisons against K2 and references, trajectory leakage audit, claim-readiness |
| `support_reliability_diagnostics` | Implemented | `mbp.analysis.support_reliability` | interval table path, existing capacity-horizon predictions, existing threshold-warning predictions, existing policy pairwise metrics, train-only support distances, selective retention curves, claim-readiness |
| `diagnostic_state_distillation_report` | Implemented | `mbp.baselines.diagnostic_state_distillation` | capacity horizon table path, threshold-warning table path, PULSE target table path, EIS target table path, auxiliary model level, inner grouped OOF folds, downstream D0-D3 metrics, leakage audit, claim-readiness |
| `main_project_synthesis_v2` | Implemented | docs/reports synthesis | source claim ledger, experiment memos, gate-status table, blocked-claim review, next-branch decision, source-consistency check |

## Gate 2/3 Schema Contracts

- `MODALITY_TABLE_LOG_AGE_SCHEMA` contains reduced operating-log signals plus nullable inserted diagnostics. The inserted diagnostic values are not interval features by default.
- `CONDITION_TABLE_SCHEMA` includes `voltage_window_family`, derived from cyclic voltage limits or calendar idle SOC, so voltage/SOC-window holdouts no longer rely on scalar `age_soc`.
- `INTERVAL_TABLE_SCHEMA` is one row per adjacent `checkup_event_table` transition. It includes condition metadata, split labels, prior/post capacity targets, LOG_AGE exposure summaries, masked diagnostic-row counts, monotonicity violation counts/drop magnitudes, `LOG_AGE_monotonicity_clean`, quality flags, and schema provenance.
- `SPLIT_REGISTRY_SCHEMA` keeps replicates of each 76-parameter condition triplet grouped for headline validation and includes `voltage_window_holdout_fold`. The legacy `soc_window_holdout_fold` is retained as a compatibility alias populated from the corrected voltage-window semantics.
- `INTERVAL_SUBSET_REGISTRY_SCHEMA` defines strict/tolerant clean interval labels, sensitivity flags, exclusion reasons, and the monotonicity policy version used for baseline readiness.
- `INTERVAL_STRESS_FEATURES_SCHEMA` is a modular sidecar keyed by `cell_id`, `checkup_k`, and `checkup_k_next`. It contains LOG_AGE-derived scalar dwell, current, SOC, coupled-stress, coverage/gap, event-segmented, and diagnostic normalized-rate fields. Target-derived normalized-rate fields are excluded from F5-F13 predictive feature groups.
- `BASELINE_PREDICTION_SCHEMA` records row-level capacity predictions for the Milestone 0.5 baseline ladder and later capacity-only diagnostics. Normalized delta-rate target modes are evaluated back in `delta_capacity_Ah` units. The JSON report aggregates metrics by held-out parameter-set condition; generated prediction Parquet remains ignored by default. The report renderer also emits leaderboard CSV, baseline summary markdown, evaluation-card JSON files, diagnostic markdown, C-rate error analysis, rate-target comparisons, train-fold bias-correction tables, and plot-ready CSV tables.
- `stressor_robust_capacity_report` records non-neural stressor-axis robust HGB variants using existing capacity feature groups. It includes R0 reference, R1 condition-balanced, R2 stressor-balanced, R3 condition-bagged, and R4 train-only internal condition-selected variants, plus paired condition gains against F4 and stress R0 references. Milestone 5.3 hardens this report so missing outside-C-rate evidence cannot satisfy non-degradation, condition-bagged predictions use a stable full-training encoder with balanced per-condition row draws, exact R4 validation ties prefer R2 then R1 then R0, and zero-metric runs fail. Generated prediction Parquet remains ignored by default.
- `stressor_robust_pareto_report` records a bounded Milestone 5.4 Pareto grid over the same non-neural robust HGB family. It reports setting IDs, weight strengths, bag counts, C-rate gains, paired bootstrap p05 values, outside-C-rate degradation, nondominated frontier rows, and threshold sensitivity at fixed non-degradation cutoffs. The predeclared support rule remains R2/F8/weight=1.0 with <=5% outside-C-rate degradation; non-predeclared passing settings are diagnostic only. Generated prediction Parquet remains ignored by default.
- `stressor_robust_adaptive_report` records a Milestone 5.5 train-only adaptive selector over R2 stressor-balanced HGB weight strengths. For each outer grouped split, the selector scores candidate weights using only outer-training rows and inner grouped validation, records the selected weight and selection diagnostics, and evaluates the chosen model on untouched outer held-out rows. The conservative selector can support only a narrow adaptive robust-selection diagnostic if C-rate gain, paired support, and <=5% outside-C-rate non-degradation all pass. Generated prediction Parquet remains ignored by default.
- `stressor_robust_adaptive_replication_report` records a Milestone 5.6 replication and policy-sensitivity layer for the train-only adaptive selector. It includes logical seed rows, effective fit seed metadata, deterministic seed-reuse mode for the HGB/no-bagging path, conservative versus max-gain policy sensitivity, outside-split degradation summaries, and train/test plus inner train/validation leakage audit counts. It writes tracked JSON/CSV/Markdown reports only; no generated prediction Parquet is emitted.
- `stressor_robust_attribution_report` records a Milestone 5.7 decomposition over D0 R0/F4, D1 R0/F8, D2 adaptive R2/F4, and D3 adaptive R2/F8 arms. It reports reweighting-only, raw F8, incremental F8-under-adaptive, combined adaptive F8, and adaptive-versus-raw F8 comparisons by target/split, plus outside-C-rate degradation and adaptive selection leakage audit rows. The report can support only diagnostic attribution wording when the incremental F8 comparison passes C-rate gain and paired support but fails the unchanged outside-C-rate guardrail. Generated prediction Parquet remains ignored by default.
- `stressor_robust_arm_selector_report` records a Milestone 5.8 targeted router over existing attribution arms. The report-based mode recombines existing outer-fold attribution predictions and uses D2 adaptive R2/F4 only for C-rate transfer when the train-only D2 adaptive guardrail passes, while non-C-rate stressor views route to D0 R0/F4. It reports final selector-vs-D0 comparisons, outside-C-rate degradation, selected arm rows, and leakage-audit counts. Generated prediction Parquet remains ignored by default. The schema supports only targeted diagnostic routing wording, not broad robust-capacity or policy claims.
- `hierarchical_replicate_capacity_report` records a Milestone 5.9 L5-style non-neural hierarchical capacity comparator. It includes H0 global train mean, H1 ridge, H2 train-residual partial-pooling ridge, H3 HGB reference, H4 train-residual partial-pooling HGB, and H5 replicate-variance interval diagnostics. Residual offsets, shrinkage, and interval radii are computed from outer-training rows only; parameter-set random effects are forbidden and held-out parameter sets fall back to stressor-family or global train residual offsets. Generated prediction Parquet remains ignored by default. The schema supports only hierarchical diagnostic wording, not global robust-capacity, calibrated-uncertainty, architecture, or policy claims.
- `stressor_robust_forensics` records split-level and condition-level degradation diagnostics from an existing stressor-robust capacity report and prediction Parquet. It is a failure-analysis artifact only and does not relax robust-capacity guardrails.
- `PULSE_TARGET_TABLE_SCHEMA` is one row per interval for the canonical PULSE target context. It records RT/50% SOC resistance at check-up `k` and `k+1`, resistance deltas, alignment deltas, quality flags, schema version, and target direction handling in metadata. The canonical direction handling is currently `mean`; `charge` and `discharge` sidecars are diagnostic.
- `pulse_alignment_sensitivity_report` records canonical alignment-delta summaries and retained interval/cell/parameter-set/split coverage under 6h, 12h, 24h, 36h, and all-row thresholds.
- `pulse_missingness_reports` record missing canonical PULSE endpoints by interval, parameter-set condition, C-rate holdout fold, and profile holdout fold.
- `PULSE_PREDICTION_SCHEMA` records row-level scalar PULSE resistance baseline predictions. Generated prediction Parquet remains ignored by default; JSON/CSV/markdown report artifacts are trackable.
- `pulse_target_robustness_report` reuses the scalar PULSE baseline report schema and adds rendered comparison CSVs for target family, 1s-vs-10ms behavior, and delta-vs-k1 behavior.
- `capacity_pulse_coupling_table` is one row per interval with finite capacity targets and canonical PULSE targets. It includes condition metadata, split labels, LOG_AGE EFC/calendar controls, quality flags, and schema version.
- `pulse_capacity_coupling_diagnostics` joins capacity prediction residuals to canonical PULSE growth and reports Pearson/Spearman correlations plus grouped C-rate/cold-rate summaries. It is a prediction-row diagnostic only and does not authorize multimodal claims.
- `pulse_capacity_coupling_robustness` filters to one canonical capacity model/feature/target, then reports interval-level and condition-level correlations, parameter-set bootstrap summaries, residualized confound-control correlations, subgroup summaries, and coupling claim-readiness.
- `prior_pulse_capacity_predictive_comparison` compares F4 against the best prior-PULSE feature group on the same PULSE-covered interval population. It reports paired condition-level gains, split-level bootstrap intervals, coverage effects, and claim-readiness. Future PULSE state and PULSE deltas are forbidden as capacity inputs.
- `prior_pulse_vs_best_nonpulse_comparison` compares the best prior-PULSE HGB group against the strongest supplied non-PULSE HGB group by target/split on the same PULSE-covered interval population. It reports paired condition-level gains, bootstrap intervals, and claim-readiness.
- `EIS_FEATURE_TABLE_V1_SCHEMA` is one row per cell/check-up/SOC/temperature context for the canonical EIS feature policy. The v1 sidecar records selected-frequency E1 features, nullable R0/R1 placeholders with explicit provenance/leakage fields, E3 geometric Nyquist summaries, valid-frequency coverage, alignment delta, quality flags, schema version, and feature policy version.
- `eis_qa_report` audits EIS coverage by SOC, RT/OT context, cell, check-up, and split metadata; reports valid modeling frequency/fraction distributions; writes alignment summaries; and verifies the charter mask excludes 100 Hz, 208.3 Hz, and 14.7 kHz while retaining finite valid 0.5-5000 Hz values.
- `eis_feature_qa_report` checks the canonical RT/50% feature table for row/cell/condition coverage, selected-frequency completeness, feature NaN counts, valid modeling fraction, split coverage, and warnings. It does not authorize EIS predictive claims.
- `EIS_TARGET_TABLE_V1_SCHEMA` is one row per interval with canonical RT/50 EIS features at check-up `k` and `k+1`, EIS delta targets, valid modeling fractions, alignment deltas, split labels, quality flags, schema version, and feature policy version. Future EIS `k1` features and EIS deltas are forbidden as capacity/PULSE inputs.
- `eis_scalar_baseline_report` records grouped scalar EIS endpoint baselines for selected-frequency and geometric Nyquist targets. It is an EIS self-diagnostic baseline and does not by itself authorize EIS improvement claims.
- `prior_eis_vs_best_noneis_comparison` compares best prior-EIS PULSE/capacity HGB groups against strongest supplied non-EIS HGB groups on the same EIS-covered prediction population. It reports paired condition-level gains, parameter-set bootstrap intervals, C-rate summaries, and conservative claim-readiness.
- `eis_alignment_feature_sensitivity` records whether 24 h/36 h EIS alignment thresholds and selected-frequency/valid-fraction filters change prior-EIS comparison conclusions.
- `eis_leakage_audit` confirms non-EIS target baselines use only prior EIS `k` scalar features and exclude EIS `k1`, EIS deltas, R0/R1 without leakage-safe provenance, DRT fields, and learned embeddings.
- `semi_empirical_capacity_report` records non-neural ridge-style semi-empirical stress comparators. Generated prediction Parquet remains ignored; JSON/CSV/Markdown reports are trackable.
- `replicate_uncertainty_diagnostics` records condition-triplet spread, empirical min/max tolerance intervals, model error versus replicate spread, C-rate replicate uncertainty, and conservative uncertainty claim-readiness.
- `capacity_calibration_diagnostics` records raw HGB quantile coverage, split-conformal coverage, stressor-family conformal coverage, replicate-hybrid interval diagnostics, coverage by split/condition, width summaries, C-rate calibration summaries, and conservative calibration claim-readiness. After Milestone 5.2, raw L3 quantile endpoints are noncrossing post-sort diagnostics and still do not authorize calibrated-uncertainty wording.
- `RUN_EVENT_TABLE_V1_SCHEMA` records LOG_AGE-derived contiguous charge/discharge/rest/unknown event segments for each interval. The builder streams the full LOG_AGE table by row group and writes events in Parquet batches to avoid materializing the source or event table in memory.
- `INTERVAL_SEQUENCE_FEATURES_V1_SCHEMA` is one row per interval with aggregate event counts/durations, order-aware transition and stress-position summaries, and deterministic shuffled-order controls. It excludes target-derived capacity rates.
- `sequence_value_diagnostics` compares aggregate event features, order-aware features, shuffled-order controls, and timestamp-weighted stress baselines under grouped non-neural capacity validation. It is a falsification gate and does not authorize sequence models unless order-aware features beat aggregate and shuffled controls.
- `INTERVAL_EVENT_SEQUENCE_TABLE_V1_SCHEMA` is one row per interval with fixed-length true and shuffled LOG_AGE run-event vectors plus an event mask. The v1 policy samples at most 64 events per interval deterministically from the streamed run-event table, records truncation counts, and keeps generated Parquet ignored by default.
- `minimal_sequence_reopening_report` records the Milestone 7.1 true/shuffled fixed-length sequence comparison. Ridge rows are non-neural diagnostics; Torch MLP rows are valid only when CUDA is available and CPU fallback is disabled. The report can reopen a later narrow sequence baseline only if true sequence beats shuffled, aggregate-event HGB, and timestamp-stress HGB controls under grouped C-rate and non-C-rate views.
- `POLICY_CONTRAST_REGISTRY_V1_SCHEMA` records Milestone 7.2 matched observed condition contrasts. Each row varies one family field (`nominal_charge_C_rate`, `nominal_temperature_C`, `voltage_window_family`, or `profile_label`) while matching the remaining nominal fields for that family. The schema records arm parameter sets, arm values, replicate-cell counts, interval-row counts, common check-up support, triplet support, and support quality. Generated Parquet remains ignored by default.
- `policy_contrast_support_diagnostics` records Milestone 7.2 observed support and sign-stability diagnostics. It writes family support, observed capacity-loss stability rows, feasibility/readiness markdown, and a JSON summary. These artifacts support only observed support diagnostics and possible future feasibility planning; they do not authorize policy ranking, policy recommendation, causal claims, same-cell counterfactual claims, CBAT, or neural/sequence branches.
- `policy_ranking_feasibility_diagnostics` records Milestone 7.3 report-only supported contrast-ordering checks. It joins `policy_contrast_registry_v1`, `capacity_horizon_table_v1`, and existing `capacity_horizon_l0_l2_predictions.parquet`, computes pairwise observed-versus-predicted sign and magnitude metrics, bootstraps HGB K2 sign-accuracy gains over reference baselines by contrast ID, and keeps K3 rows labeled `oracle_diagnostic_only`. The schema supports only partial/diagnostic observed contrast-ordering wording and does not authorize policy recommendation, causal claims, same-cell counterfactuals, calibrated policy risk/utility, CBAT, or sequence/neural branches.
- `KNEE_CANDIDATE_TABLE_V1_SCHEMA` records one candidate knee row per cell, detector, x-axis, and smoothing policy. It includes detector quality flags, slopes, slope-change ratio, SOH at knee, condition metadata, and schema version.
- `KNEE_RISK_LABEL_TABLE_V1_SCHEMA` records exploratory interval-level knee-risk labels from the primary detector policy. It is a label-readiness sidecar only and does not authorize knee prediction.
- `knee_label_stability_diagnostics` records detector pair agreement, x-axis sensitivity, smoothing sensitivity, replicate-triplet knee spread, condition-level knee summaries, and knee claim-readiness.
- `KNEE_STABLE_CONDITION_REGISTRY_V1_SCHEMA` records whether each parameter-set condition has stable, unstable, or insufficient primary detector knees. The default stable rule requires at least two valid replicate knees, spread no greater than two check-ups, and no severe trajectory QA flags.
- `THRESHOLD_EVENT_LABEL_TABLE_V1_SCHEMA` records exploratory interval-level threshold-event labels for 80%, 70%, and 60% SOH/capacity-relative thresholds. These labels are target-readiness artifacts only and do not authorize prediction models.
- `knee_threshold_label_forensics` records inconsistent primary-knee conditions, stable-condition coverage, threshold-event replicate consistency, and a knee-vs-threshold decision memo.
- `THRESHOLD_WARNING_TABLE_V1_SCHEMA` records one prospective warning row per cell/check-up for the selected threshold label. It includes only state, time, cumulative exposure, nominal metadata, split labels, and event-horizon labels known from the target table; it excludes `capacity_Ah_k1`, capacity deltas, future interval exposure summaries, future PULSE/EIS state, and PULSE/EIS deltas as features.
- `threshold_warning_baseline_report` records grouped non-neural classifier metrics for `capacity_below_80pct_initial` warning horizons, including event-rate, distance-to-threshold, prior-only extrapolation, logistic proximity, logistic/ridge, and HGB classifier rows. It reports fixed-width and equal-frequency ECE side by side. Probability outputs are clipped consistently before scoring and writing report rows. Generated prediction Parquet remains ignored by default; JSON/CSV/Markdown report artifacts are trackable.
- `threshold_warning_hardening_diagnostics` records lead-time performance, proximity-bin performance, C-rate lead-time performance, censoring-policy counts, reliability bins, split-level calibration, and C-rate calibration summaries. These diagnostics report both fixed-width and equal-frequency ECE, harden the threshold-warning result, and do not authorize calibrated risk.
- `threshold_warning_censoring_sensitivity` compares all-row and verified-only label policies for event-rate, logistic proximity, and HGB W2 rows. It records target row counts, positive/negative counts, grouped metrics, C-rate metrics, and whether both policies pass the diagnostic claim rule.
- `threshold_warning_final_claim_readiness` records the final threshold-warning decision. It can support a narrow diagnostic threshold-event forecasting claim while keeping calibrated risk, detector-knee prediction, causal early-warning claims, policy ranking, and CBAT blocked. Milestone 5.2 adds equal-frequency ECE to the C-rate claim matrix without changing the decision.
- `threshold_warning_probability_calibration` records grouped post-hoc calibration diagnostics for the existing HGB W2 threshold-warning model. It reserves calibration conditions from the non-test side of each grouped split, evaluates raw HGB W2, Platt/logistic calibration, and isotonic calibration under all-row and verified-only label policies, reports fixed-width and equal-frequency ECE sensitivity, and keeps generated calibrated prediction Parquet ignored by default. Milestone 5.3 hardens this schema/report by attaching Parquet schema metadata, using the unpenalized logistic convention for Platt scaling, requiring both all-row and verified-only policies for strict readiness, checking C-rate ECE per label policy, and preventing fallback-raw calibration rows from passing strict calibrated-risk readiness.
- `CAPACITY_HORIZON_TABLE_V1_SCHEMA` records observed multi-check-up capacity targets for horizons 1, 2, 3, and 5. It includes check-up-k capacity/SOH/time/cumulative history, prior-only slope fields, nominal condition metadata, split labels, and target `capacity_Ah_kh` / `delta_capacity_Ah_h`. The k-to-k+h exposure fields are retained only for K3 oracle diagnostics and are not prospective forecast inputs.
- `CAPACITY_HORIZON_TRAJECTORY_FEATURES_V1_SCHEMA` records a prior-only trajectory-shape sidecar keyed to capacity horizon rows. It includes lagged prior capacity deltas, rolling prior-delta statistics, prior slope, curvature, volatility, capacity-increase counts, and quality flags derived only from intervals ending at or before check-up `k`.
- `capacity_horizon_baseline_report` records grouped non-neural multi-horizon forecasting metrics for persistence, prior-slope extrapolation, Ridge, and HGB models over K0-K3 feature groups. K0-K2 are prospective; K3 is oracle-diagnostic only. Generated prediction Parquet remains ignored by default.
- `capacity_horizon_forensics_report` records Milestone 6.1 report-based diagnostics for the Milestone 6.0 multi-horizon run. It renders HGB K2 gains against persistence and prior-slope references by split, C-rate condition-level error rows, prior-slope failure bins, K3 oracle-exposure gains by split, and a prospective feature audit. It adds no new model training and does not strengthen claims by itself.
- `capacity_horizon_trajectory_diagnostics` records Milestone 6.2 K4/K5 trajectory-shape comparisons against K2, persistence, and prior-slope references. It keeps trajectory shape claim-gated and blocks broad forecasting, architecture, policy, causal, calibrated-risk, and calibrated-uncertainty claims when horizon-3 capacity repair or C-rate preservation fails.
- `policy_ranking_failure_forensics` records Milestone 7.4 report-only diagnostics over existing Milestone 7.3 policy-ranking CSV artifacts. It includes effect-size-thresholded sign accuracy, HGB-vs-prior-slope failure bins, rank-correlation rows, top-k/regret rows, and claim-readiness guardrails. It excludes K3 oracle rows from prospective readiness and does not authorize policy recommendation, causal/same-cell counterfactual claims, calibrated policy risk/utility, CBAT, or sequence/neural branches.
- `support_reliability_diagnostics` records Milestone 8.0 report-only support-aware selective reliability diagnostics over existing capacity-horizon, threshold-warning, and supported policy-contrast prediction artifacts. It computes train-only condition-support distances per grouped split and retention curves by support score. The schema supports only support/audit and abstention diagnostics; it does not authorize calibrated risk, deployment reliability, policy recommendation, causal/same-cell counterfactual claims, CBAT, or architecture readiness.
- `diagnostic_state_distillation_report` records Milestone 8.1 leakage-safe non-neural diagnostic-state distillation. Stage A predicts current PULSE/EIS scalar diagnostic targets from check-up-k state/time/nominal fields using train-only inner grouped out-of-fold predictions for outer-training rows and outer-train-only fits for held-out rows. Stage B compares D0 capacity-state reference features against D1 predicted PULSE state, D2 predicted EIS state, and D3 predicted PULSE+EIS state for capacity-horizon and threshold-warning downstream tasks. The report includes auxiliary surrogate accuracy, downstream gains, C-rate diagnostic-state gains, prediction-mode counts, leakage audit, and claim-readiness. Generated prediction Parquet remains ignored by default and the schema supports only diagnostic-state distillation wording, not CBAT, broad multimodal architecture, calibrated risk/uncertainty, policy, causal, or same-cell counterfactual claims.
- `DIAGNOSTIC_HORIZON_TABLE_V1_SCHEMA` records Milestone 8.2 multi-horizon scalar diagnostic endpoint targets for PULSE and EIS. It is long-format by `cell_id`, `checkup_k`, `target_checkup_k`, `horizon_checkups`, diagnostic family, and target name. It stores current diagnostic value at check-up `k`, observed future diagnostic target at `k+h`, diagnostic delta for reporting, check-up-k capacity/SOH/time/cumulative state, prior-only capacity slope fields, nominal condition metadata, grouped split labels, quality flags, and schema version. The schema supports diagnostic endpoint forecasting only; future diagnostic targets, diagnostic deltas, future interval exposure, and event labels are forbidden as model features. Generated Parquet remains ignored by default.
- `diagnostic_horizon_baseline_report` records Milestone 8.2 grouped non-neural PULSE/EIS endpoint forecasting baselines. It includes persistence, train-mean, Ridge, and HGB model levels over DH0-DH3 feature groups, split-level metrics, aggregate leaderboard rows, reference-gain rows, leakage audit, and claim-readiness. Generated prediction Parquet remains ignored by default. The schema supports only partial diagnostic endpoint-forecasting wording when the strict primary and C-rate guardrails do not pass, and does not authorize CBAT, broad multimodal architecture, calibrated risk/uncertainty, policy ranking, causal, sequence/neural, or same-cell counterfactual claims.
- `benchmark_tasks_v1` records the frozen task registry for the completed benchmark. It maps each task to a primary claim ID, status, targets, split views, metrics, source artifacts, local ignored data artifacts, allowed wording, forbidden wording, and decision.
- `benchmark_task_registry_check` records executable validation of `benchmark_tasks_v1` against the claim ledger, claim matrix, source artifacts, ignored-data policy, and blocked-claim guardrails. It produces task-level leaderboard, task-card, and model-card synthesis artifacts without training models.
- `main_project_synthesis_v2` records the post-2.6.2 technical checkpoint: refreshed claims, gate status, blocked claims, next-branch decision, and source consistency. It is documentation-only and does not create models, features, or generated data products.
