# Table 03: Split Difficulty

Source artifact:

- `reports/synthesis/split_difficulty_summary.csv`

Columns:

- split;
- best known `capacity_Ah_k1`;
- best known `delta_capacity_Ah`;
- best known `delta_pulse_1s_resistance`;
- qualitative difficulty;
- source artifact;
- note.

Intended claim:

- C-rate is the hardest capacity split, while best-known rows are descriptive
  and do not override paired claim-readiness tests.

Caption draft:

> Split difficulty summary. Best-known rows orient the reader across reports,
> but paper-facing claims follow paired comparisons and the claim ledger.
