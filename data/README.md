# Data Directory

Raw and derived battery data must not be committed.

- `raw/`: downloaded archives and extracted raw files, gitignored
- `interim/`: intermediate parser outputs, gitignored
- `processed/`: versioned data products, gitignored
- `manifests/`: audit manifests and SHA-256 records
- `splits/`: future frozen split registries

